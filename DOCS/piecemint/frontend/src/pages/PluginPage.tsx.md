# Per-plugin dashboard

**Source file:** `piecemint/frontend/src/pages/PluginPage.tsx`

## Overview

- Dynamic route **`/plugin/:pluginId`**: settings panels (invoice, tax, email, stockholders, etc.) when plugin is installed.
