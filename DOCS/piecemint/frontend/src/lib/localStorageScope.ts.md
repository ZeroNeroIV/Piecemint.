# Namespaced localStorage keys

**Source file:** `piecemint/frontend/src/lib/localStorageScope.ts`

## Overview

- Migrates legacy localStorage key suffixes; single-workspace deployments use unprefixed keys.
