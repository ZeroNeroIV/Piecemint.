# Pipenv lockfile

**Source file:** `piecemint/backend/Pipfile.lock`

## Overview

- Resolved dependency graph; use **`pipenv install --deploy`** in CI for reproducible installs.

- **Do not hand-edit**; regenerate with **`pipenv lock`** after changing the Pipfile.
