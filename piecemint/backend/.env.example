# Google Generative AI (Smart Categorizer plugin — smart_categorize)
# Create a key: https://aistudio.google.com/apikey
GOOGLE_API_KEY=
GEMINI_MODEL=gemma-4-31b-it

# Email notifications plugin — outbound SMTP (optional if users save SMTP in the app)
# FF_SMTP_HOST=smtp.example.com
# FF_SMTP_PORT=587
# FF_SMTP_USER=
# FF_SMTP_PASSWORD=
# FF_SMTP_FROM=
# FF_SMTP_USE_TLS=1
# App-saved SMTP JSON: backend/plugins/email_notifications/data/smtp_settings.json (gitignored)

# Web notifications plugin — reserved for future Web Push
# FF_VAPID_PUBLIC_KEY=
