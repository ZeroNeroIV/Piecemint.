# Optional. If unset during `npm run dev`, the UI uses `/api` and Vite proxies to
# http://127.0.0.1:8000 (see vite.config.ts). Start the API with:
#   npm run piecemint-be
#
# Use a full URL when the API is elsewhere (e.g. another machine):
# VITE_API_URL=http://192.168.1.10:8000/api
